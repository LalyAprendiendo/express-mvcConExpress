import express, { json } from 'express';

const PORT = 8080;
const app = express();

// MIDDLEWARES



// ROUTES 



// SERVER 

app.listen(PORT, () => {
	console.log('Server listening on port', PORT);
});
